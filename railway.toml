[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn \"app:create_app()\" --workers 1 --threads 4 --bind 0.0.0.0:$PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[volumes]]
mountPath = "/data"
